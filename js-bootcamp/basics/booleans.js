let temp = 18 
let isFreezing = temp < 32

isFreezing

// console.log(isFreezing)

// === is the equality operator
// !== are not equal operator
// < less than operator
// > greater than operator

if(true) {} //code will always run
if (false) {} //code will not run

if(isFreezing) {
    console.log("It's freezing outside!")
}